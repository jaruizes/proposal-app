package io.github.jaruizes.proposal.infrastructure.client.presentation; import io.github.jaruizes.proposal.domain.ports.PresentationPort; import org.springframework.stereotype.Component; import java.util.UUID; @Component public class StubPresentationAdapter implements PresentationPort { public PresentationResult materialize(UUID offerId,String slidesPlan,String outputFolder,String documentName){var id="pending-google-slides-"+offerId;var report="# Presentation build report\n\nPhase 5 orchestration is wired, but Google Slides materialization is still an adapter boundary in this first backend version.\nThe approved slides-plan was validated and preserved. Configure/replace PresentationPort with the Google Workspace/MCP adapter next.\n";return new PresentationResult(id,"about:blank#"+id,report);} }
