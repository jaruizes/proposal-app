# Proposal document templates

`builtin-neutral` is generated programmatically and requires no binary template.

Corporate DOCX templates can be mounted or copied into this directory using the
name `<template-id>.docx`. The renderer preserves the template's styles,
headers, footers and section configuration while replacing body content with the
canonical proposal Markdown.

Only fonts that may legally be distributed should be baked into the image.
